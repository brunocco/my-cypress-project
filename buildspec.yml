version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Instalando dependências..."
      - npm install
  build:
    commands:
      - echo "Executando testes Cypress..."
      - npx cypress run

artifacts:
  files:
    - "**/*"
