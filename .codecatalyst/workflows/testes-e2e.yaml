SchemaVersion: 1.0
Name: Rodar-Testes-Cypress

Triggers:
  - Type: Push
    Branches:
      - main

Actions:
  RodarCypress:
    ActionType: AWS::CodeBuild::Build
    Inputs:
      Sources:
        - WorkflowSource
    Configuration:
      Runtime: nodejs18
      Commands:
        - echo "Instalando dependências..."
        - npm install
        - echo "Executando testes Cypress..."
        - npx cypress run



